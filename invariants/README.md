# 不变量库

只有在至少两个彼此独立的真实场景里证明过价值的不变量，才能放进这里。
在被证明之前，先留在具体项目的检查代码里，不要提前抽象。

## 分类

- `value_predicates/` —— 能用线性算术谓词表达的（非负、上界、单调、非空）
  直接复用core.py/core2.py现有机制
- `sequencing/` —— 涉及顺序/状态/生命周期的（open先于close、必须先初始化）
  现有机制表达不了，需要typestate，在真正需要之前不要动
