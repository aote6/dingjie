# 定界 (dingjie) - 项目状态

最后更新：2026-07-25

## 这是什么

一个用精化类型(refinement types) + 效应系统(effect system)做"执行前契约检查"的最小原型。
核心问题：能不能在一段代码真正跑之前，就用数学方法证明它不会违反约定的前提条件，
而不是靠事后测试/人工审查。

## 当前状态：能用，已在真实项目里拦住过真实bug

- `core.py` —— 单变量精化类型 + 效应检查，3个测试场景全部通过
- `core2.py` —— 多变量关系契约(转账场景)，区分"具体值检查"(永远可判定，代入求值)
  和"抽象类型检查"(symbolic推理，受限于可判定片段)，5个场景全部通过
- 已集成进 `~/unbounded/inventory.py` 的 `add()`/`remove()`，
  修复了一个真实存在的bug：`remove(item, count=负数)` 会变相增加物品数量，
  现在会抛出 `ContractViolation` 被拦下

## 已验证的数学边界（不是猜测，是实测+证明）

- 精化类型的谓词限制在【线性整数算术】内 —— 可判定，Presburger(1929)证明过，
  `core.py`/`core2.py`的所有测试都在这条边界内，真实可靠
- 一旦谓词涉及【两个变量相乘】—— 进入丢番图方程范畴，
  希尔伯特第十问题(Matiyasevich, 1970证明)：不存在通用判定算法
  详见 `boundary_test.py`

## 架构原则

- 运行时契约检查用纯Python条件判断（快，嵌入被检查函数本身）
- sympy符号推理只用于设计期验证，不用于每次实际调用（避免性能问题）
- 检查器自己的正确性不能想当然：第一版用`sp.satisfiable()`误判过，
  换成`sp.simplify()`才对，这个坑是真实踩过、真实修复的

## 明确不做的事（防止范围失控）

以下方向理论上都成立，但现阶段明确排除，避免变成追不完的研究项目：
- 不做字符串/数组的精化类型（需要另一套可判定性理论）
- 不做效应多态（高阶函数的效应推导，Koka花了十年才做稳，不是现阶段目标）
- 不追求"类型检查器自身经过形式化证明"（可信计算基问题，研究级课题）
- 目标始终是：能不能在无界/灵体的真实代码里再拦住几个真实bug，
  不是把这个原型长成一门通用语言

## 下一步候选

- 把同样的模式用到坐标越界检查（无界项目里已知的风险点）
- 把同样的模式用到灵体的布局坐标校验

## 许可证

AGPL-3.0 —— 选择理由：普通GPL在"改了之后包成网络服务卖"这一步不触发开源义务，
AGPL堵上了这个口子。


## 2026-07-27 更新：三值逻辑与有界枚举判定器

### 发现并修复的核心缺陷

core2.py 的 check_transfer_symbolic 存在一个架构层面的 bug：

    simplified = sp.simplify(counterexample)
    ok = (simplified == False)

这行代码把 sp.simplify 的"化简失败（返回原式）"等价为"存在反例"。
当契约包含两个自由变量相乘时，sp.simplify 对这类非线性表达式没有判定能力——
它只是把式子原样吐回来。结果是：本来恒成立的合法契约被误判为"拒绝"。

这不是 SymPy 的 bug，是接口设计的缺陷——判定器明明"不知道答案"，
接口却强制它给一个"是/否"。

### 修复方案：三值逻辑 + 枚举降级

引入 ProofResult 三值协议：

- PASS — 已证明契约恒成立
- VIOLATION — 已找到具体反例
- UNKNOWN — 判定器能力不足，诚实放弃

ProofResult 禁止 bool() 隐式转换，逼迫调用方显式处理三种情况。

判定链路改为：

契约 -> sympy 线性推理 -> 明确？-> 直接返回 PASS/VIOLATION
              | 不明确
         提取边界 -> 有界？-> EnumSolver 穷举 -> PASS/VIOLATION
                        | 无界
                      UNKNOWN

### 新增文件

- proof_result.py — ProofResult 枚举定义
- enum_solver.py — 有界枚举判定器（Fast-Fail + 算力熔断）
- core2.py — 已覆盖为三值版本（原二值版由 core2_v2.py 演进而来）

### Kaggle 算力验证结果

50万条随机二变量乘法契约，在 100x100 范围内穷举：

- VIOLATION: 500,000 条（全部在有限范围内确定判定）
- UNKNOWN: 0 条
- 耗时: 65.80 秒（7,598 条/秒）

多级复杂度探针测试：

| 复杂度 | 变量数 | 状态空间 | 耗时 | 结果 |
|--------|--------|----------|------|------|
| L1: a*b<=cap | 2 | 10,000 | 0.0012s | 确定 |
| L2: a*b+b*c<=cap | 3 | 1,000,000 | 0.1584s | 确定 |
| L3: a*b+c*d<=cap | 4 | 6,250,000 | 0.9850s | 确定（临界） |
| L4: a*b*c<=cap | 3 | 1,000,000 | 0.1481s | 确定 |
| L5: a*b+b*c+c*d*e<=cap | 5 | 312,500,000 | 1.0019s | UNKNOWN（熔断） |

### 结论

1970年 DPRM 定理的"墙"确实存在——5变量3亿状态空间时，枚举法触达算力边界，
三值熔断生效，返回 UNKNOWN。

但这堵墙离真实游戏业务非常远。游戏逻辑中 99% 的契约（2-3变量，范围在几百以内）
全部落在 0.001s~0.15s 的安全统治区，枚举法就是事实上的通用判定器。

这不是推翻定理——是证明了你的问题根本不需要走到定理管的那块地盘。
